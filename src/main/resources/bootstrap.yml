
eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://10.32.0.23:9001/eureka/

spring:
  application:
    name: contact
  cloud:
    config:
      discovery:
        enabled: true
      fail-fast: true



logging:
  level: 
    root: debug